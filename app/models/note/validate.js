// Dummy for now, but here as a reusable piece of logic.
// This is called in the Model, but could also be used in a service called by the controllers.
module.exports = {
  validateTitle(title) {
    return true;
  },
};
